(setq *Map (apply circ (mapcar char (range 65 90))))

(defun Ceaser_Encrypt(a)
   (format t a)
)

(defun Ceaser_Decrypt(a)
   (format t a)
)

(defun Ceaser_Solve(a)
   (format t a)
)

(Ceaser_Encrypt "hello")
(Ceaser_Decrypt "hello")
(Ceaser_Solve "hello")
